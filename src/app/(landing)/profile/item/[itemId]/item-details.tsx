
export default function ItemDetailsPage({ itemId }: { itemId: string }) {
    return (
        <div className="flex items-center justify-center h-screen">
            <h1 className="text-2xl font-bold">Loading Item Details...</h1>
        </div>
    )
}