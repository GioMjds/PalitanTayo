import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Verify OTP - Palitan Tayo!",
    description: "Verify your OTP code",
}

export default function VerifyOTP() {
    return (
        <h1>Verify OTP</h1>
    )
}